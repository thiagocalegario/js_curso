const numbers = [1,2,3,4,5]

//Retorna -1 caso não tenha ocorrencia do caractere procurado
console.log(numbers.indexOf(0))

//Retorna o index do caractere destro do array, caso ele exista
console.log((numbers.indexOf(1)))

//Checar se um elemento existe em um array
console.log(numbers.includes(0))